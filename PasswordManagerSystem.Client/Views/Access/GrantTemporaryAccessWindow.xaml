<Window x:Class="PasswordManagerSystem.Client.Views.Access.GrantTemporaryAccessWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Alkalmi hozzaferes"
        Width="700"
        Height="500"
        MinWidth="700"
        MinHeight="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="#0B101A"
        Foreground="#E5E7EB">

    <Window.Resources>
        <Style x:Key="DarkButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Background" Value="#1F2937"/>
            <Setter Property="Foreground" Value="#E5E7EB"/>
            <Setter Property="BorderBrush" Value="#374151"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#374151"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#111827"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource DarkButtonStyle}">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#2563EB"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#1D4ED8"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#1D4ED8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#1E40AF"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#1E40AF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="#E5E7EB"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="DarkListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Background" Value="#111827"/>
            <Setter Property="Foreground" Value="#E5E7EB"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="Transparent"
                                BorderThickness="1"
                                Padding="10,8">
                            <ContentPresenter HorizontalAlignment="Stretch"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#1F2937"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#1E3A8A"/>
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#38BDF8"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="IsKeyboardFocusWithin" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="ItemBorder" Property="Background" Value="#2563EB"/>
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#7DD3FC"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#1F2937"
            BorderThickness="1"
            Background="#0B101A">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"
                  Background="#0B101A"
                  MouseLeftButtonDown="TitleBar_OnMouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="44"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Alkalmi hozzaferes"
                           Margin="16,0,0,0"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Foreground="#E5E7EB"/>

                <Button Grid.Column="1"
                        Content="X"
                        Click="CancelButton_OnClick"
                        Background="Transparent"
                        Foreground="#E5E7EB"
                        BorderThickness="0"
                        FontWeight="SemiBold"
                        Cursor="Hand"/>
            </Grid>

            <Border Grid.Row="1"
                    Margin="18"
                    Padding="22"
                    Background="#111827"
                    CornerRadius="12">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="Felhasznalo-alapu ideiglenes hozzaferes"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="#F9FAFB"/>

                    <TextBlock Grid.Row="2"
                               Text="BEJEGYZES"
                               FontSize="11"
                               FontWeight="SemiBold"
                               Foreground="#94A3B8"/>

                    <Border Grid.Row="4"
                            Padding="12,10"
                            CornerRadius="6"
                            Background="#1F2937"
                            BorderBrush="#374151"
                            BorderThickness="1">
                        <TextBlock x:Name="CredentialTitleText"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="#E5E7EB"
                                   TextWrapping="Wrap"/>
                    </Border>

                    <TextBlock Grid.Row="6"
                               Text="FELHASZNALO KERESESE"
                               FontSize="11"
                               FontWeight="SemiBold"
                               Foreground="#94A3B8"/>

                    <Grid Grid.Row="8">
                        <TextBox x:Name="UserSearchTextBox"
								 Height="38"
								 Padding="12,0"
								 VerticalContentAlignment="Center"
								 FontSize="13"
								 FontWeight="SemiBold"
								 TextChanged="UserSearchTextBox_OnTextChanged"
								 GotKeyboardFocus="UserSearchTextBox_OnGotKeyboardFocus"
								 PreviewMouseLeftButtonDown="UserSearchTextBox_OnPreviewMouseLeftButtonDown"
								 KeyDown="UserSearchTextBox_OnKeyDown"
								 Background="#1F2937"
								 Foreground="#F9FAFB"
								 BorderBrush="#22D3EE"
								 BorderThickness="1"
								 CaretBrush="#38BDF8"
								 SelectionBrush="#2563EB"
								 SelectionOpacity="0.45"
								 TextOptions.TextFormattingMode="Display"
								 TextOptions.TextRenderingMode="ClearType"/>

                        <Popup x:Name="UserDropdownPopup"
							   PlacementTarget="{Binding ElementName=UserSearchTextBox}"
							   Placement="Bottom"
							   AllowsTransparency="True"
							   StaysOpen="True"
							   PopupAnimation="Fade"
							   Closed="UserDropdownPopup_OnClosed">

                            <Border Width="{Binding ActualWidth, ElementName=UserSearchTextBox}"
                                    MaxHeight="230"
                                    Background="#111827"
                                    BorderBrush="#374151"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Margin="0,4,0,0">

                                <Grid>
                                    <ListBox x:Name="UserListBox"
                                             Background="#111827"
                                             Foreground="#E5E7EB"
                                             BorderThickness="0"
                                             ItemContainerStyle="{StaticResource DarkListBoxItemStyle}"
                                             MouseDoubleClick="UserListBox_OnMouseDoubleClick"
                                             SelectionChanged="UserListBox_OnSelectionChanged"
                                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                             ScrollViewer.CanContentScroll="True"
                                             VirtualizingPanel.IsVirtualizing="True"
                                             VirtualizingPanel.VirtualizationMode="Recycling">

                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>

                                                    <TextBlock Grid.Row="0"
                                                               Text="{Binding DisplayText}"
                                                               Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=Foreground}"
                                                               FontSize="13"
                                                               FontWeight="SemiBold"
                                                               TextTrimming="CharacterEllipsis"/>

                                                    <TextBlock Grid.Row="1"
                                                               Text="{Binding RoleDisplayName}"
                                                               Foreground="#94A3B8"
                                                               FontSize="11"
                                                               Margin="0,2,0,0"
                                                               TextTrimming="CharacterEllipsis"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>

                                    <TextBlock x:Name="EmptyUsersTextBlock"
                                               Text="Nincs talalat."
                                               Visibility="Collapsed"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Foreground="#94A3B8"
                                               FontSize="13"
                                               Margin="0,20"/>
                                </Grid>
                            </Border>
                        </Popup>
                    </Grid>

                    <TextBlock Grid.Row="10"
                               Text="IDOTARTAM"
                               FontSize="11"
                               FontWeight="SemiBold"
                               Foreground="#94A3B8"/>

                    <UniformGrid Grid.Row="12" Columns="4">
                        <RadioButton x:Name="OneHourRadio"
                                     Content="1 ora"
                                     Tag="1"
                                     IsChecked="True"
                                     Style="{StaticResource DarkRadioButtonStyle}"/>
                        <RadioButton Content="3 ora"
                                     Tag="3"
                                     Style="{StaticResource DarkRadioButtonStyle}"/>
                        <RadioButton Content="5 ora"
                                     Tag="5"
                                     Style="{StaticResource DarkRadioButtonStyle}"/>
                        <RadioButton Content="8 ora"
                                     Tag="8"
                                     Style="{StaticResource DarkRadioButtonStyle}"/>
                    </UniformGrid>

                    <TextBlock Grid.Row="14"
                               Text="A hozzaferes csak megtekintesi jogot ad. Irasi es torlesi jog nem kerul kiosztasra."
                               FontSize="12"
                               Foreground="#CBD5E1"
                               TextWrapping="Wrap"/>

                    <TextBlock Grid.Row="15"
                               x:Name="ValidationTextBlock"
                               Text=""
                               Visibility="Collapsed"
                               FontSize="12"
                               Foreground="#FCA5A5"
                               TextWrapping="Wrap"
                               VerticalAlignment="Bottom"
                               Margin="0,10,0,0"/>

                    <Grid Grid.Row="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="1"
                                Content="Megse"
                                Style="{StaticResource DarkButtonStyle}"
                                Click="CancelButton_OnClick"/>

                        <Button Grid.Column="3"
                                Content="Hozzaadas"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Click="ConfirmButton_OnClick"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>